---
import { Content, frontmatter } from "../content/about.md";
import BlogPost, { type Props } from "../layouts/BlogPost.astro";
---

<BlogPost
  {...frontmatter as Props}
  pubDate={new Date(frontmatter.pubDate)}
  updatedDate={new Date(frontmatter.updatedDate)}
>
  <Content />
</BlogPost>
